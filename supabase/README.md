# Supabase Local

## Prereqs
- Supabase CLI installed

## Usage
```bash
# From repo root
supabase start
# Apply migrations
supabase db reset --use-migra --local
```

Migrations live in `supabase/migrations`.

